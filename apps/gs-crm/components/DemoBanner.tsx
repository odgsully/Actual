/**
 * DemoBanner Component (GSRealty Stub)
 *
 * GSRealty doesn't have a demo mode - only real admin/client accounts.
 * This component exists as a stub to prevent build errors from old Wabbit pages.
 */

export default function DemoBanner() {
  // GSRealty doesn't use demo mode - return nothing
  return null
}
