---
import AvatarStack from './AvatarStack.astro'
---
<section id="collaboration" class="py-24 px-6">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-6">
      <h2 class="font-['Playfair_Display'] text-3xl sm:text-4xl font-bold mb-4">Built for Teams</h2>
      <p class="text-white/50 max-w-xl mx-auto mb-6">Async collaboration that respects everyone's schedule. No meetings required.</p>
      <div class="flex justify-center">
        <AvatarStack count={8} size={40} seed="team" label="Teams ranking right now" />
      </div>
    </div>

    <div class="grid md:grid-cols-3 gap-8 mt-14">
      <!-- RAVG -->
      <div class="glass-card p-6">
        <div class="flex items-center justify-between mb-4">
          <div class="w-10 h-10 rounded-xl bg-blue-500/10 flex items-center justify-center">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="rgb(96 165 250)" stroke-width="2">
              <path d="M18 20V10"></path>
              <path d="M12 20V4"></path>
              <path d="M6 20v-6"></path>
            </svg>
          </div>
          <AvatarStack count={3} size={24} seed="ravg" />
        </div>
        <h3 class="text-base font-semibold mb-2">RAVG Consensus</h3>
        <p class="text-white/40 text-sm leading-relaxed">
          Ranked Average aggregates individual scores into a single team consensus. Configurable formulas: simple mean, weighted by role, or exclude outliers.
        </p>
      </div>

      <!-- Roles -->
      <div class="glass-card p-6">
        <div class="flex items-center justify-between mb-4">
          <div class="w-10 h-10 rounded-xl bg-purple-500/10 flex items-center justify-center">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="rgb(168 85 247)" stroke-width="2">
              <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M22 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
          </div>
          <AvatarStack count={3} size={24} seed="roles" />
        </div>
        <h3 class="text-base font-semibold mb-2">Roles & Weights</h3>
        <p class="text-white/40 text-sm leading-relaxed">
          Owner, contributor, and viewer roles. Super RAVG lets the creative director apply a separate weight on top of the team's consensus.
        </p>
      </div>

      <!-- Async Progress -->
      <div class="glass-card p-6">
        <div class="flex items-center justify-between mb-4">
          <div class="w-10 h-10 rounded-xl bg-green-500/10 flex items-center justify-center">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="rgb(34 197 94)" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <polyline points="12 6 12 12 16 14"></polyline>
            </svg>
          </div>
          <AvatarStack count={4} size={24} seed="async" />
        </div>
        <h3 class="text-base font-semibold mb-2">Async Progress</h3>
        <p class="text-white/40 text-sm leading-relaxed">
          See who's ranked what in real time. Progress bars per team member. No more "did you review that batch?" Slack pings.
        </p>
      </div>
    </div>
  </div>
</section>
